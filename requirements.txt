# Core LangChain / LangGraph stack
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langgraph>=0.1.0
langchain-groq>=0.1.0
langgraph-checkpoint-sqlite>=3.0.0

# Vector DB
qdrant-client>=1.9.0

# Graph DB
neo4j>=5.20.0

# Embeddings (local ONNX models — fastembed)
fastembed>=0.3.0

# Pydantic / Settings
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Streamlit UI
streamlit>=1.35.0

# Utilities
tiktoken>=0.5.0
aiosqlite>=0.20.0
loguru>=0.7.2
requests>=2.31.0

# Dev / Testing (not needed on Streamlit Cloud)
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Backend API Server dependencies (also required for running tests/test_api.py)
fastapi>=0.100.0
uvicorn>=0.20.0
slowapi>=0.1.7

# ─────────────────────────────────────────────────────────────────
# NOTE: ebooklib and beautifulsoup4 are used only for the one-time
# local epub conversion (data/convert_epubs.py) and are NOT needed
# on Streamlit Cloud. Install them locally with:
#   pip install ebooklib beautifulsoup4
# ─────────────────────────────────────────────────────────────────
